[package]
name = "ph-sht45-hts"
version = "0.1.0-incubating.1"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Incubating unpublished async no_std Rust driver for the Sensirion SHT45 humidity and temperature sensor over abstract I2C."
repository.workspace = true
readme = "README.md"
publish = false
keywords = ["embedded", "no-std", "sht45", "hts"]
categories = ["embedded", "hardware-support", "no-std"]

[dependencies]
embedded-hal = "1.0"
embedded-hal-async = "1.0"

[dev-dependencies]
futures-lite = "2"
