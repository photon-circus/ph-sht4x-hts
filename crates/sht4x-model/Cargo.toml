[package]
name = "ph-sht4x-hts-model"
version = "0.1.0-incubating.1"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Unpublished independent behavioral model for selected SHT45 operations."
repository.workspace = true
readme = "README.md"
publish = false
# No `keywords` or `categories`: those fields classify a crate for a registry
# this package does not enter.
#
# No dependency tables either. The model is derived from the datasheet and
# depends on nothing at all, which is easier to see when the manifest says so by
# omission than when an empty `[dependencies]` header sits here.
